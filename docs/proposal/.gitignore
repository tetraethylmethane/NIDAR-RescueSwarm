# LaTeX build intermediates
*.aux
*.log
*.out
*.toc
*.lof
*.lot
*.bbl
*.blg
*.synctex.gz

# The compiled proposal IS tracked -- it is the deliverable a sponsor receives,
# not a build intermediate. Regenerate with:
#   pdflatex rescueswarm-proposal.tex   (twice, for cross-references)
!rescueswarm-proposal.pdf
