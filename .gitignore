# Python
__pycache__/
*.py[cod]
.venv/
venv/
env/
*.egg-info/

# Notebooks
.ipynb_checkpoints/

# OS / editor
.DS_Store
Thumbs.db
desktop.ini
.vscode/
.idea/
*.swp

# Office lock files (BOM spreadsheets)
~$*.xlsx
~$*.xls

# Build / logs
build/
dist/
*.log

# Flight and simulation artefacts (large; keep out of git)
*.BIN
*.tlog
*.ulg
logs/
datasets/field-campaign/

# Perception corpora and weights (GB-scale; keep out of git).
# datasets/boxes.csv is the committed provenance record and is NOT ignored --
# it is the only thing audit.py reads, which is what lets CI regenerate the
# audit on a fresh checkout where the imagery will never exist.
datasets/raw/
datasets/unified/
runs/
*.pt

# Personal learning notes -- never pushed. Working production code and team
# documents go in the repo proper; anything written to understand a topic
# lives here.
learning/
diary.txt
